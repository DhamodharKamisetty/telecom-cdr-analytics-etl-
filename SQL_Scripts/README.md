# SQL Scripts

Place all SQL artifacts here:

- Star schema DDL scripts (`FACT_CALL`, `DIM_CUSTOMER`, `DIM_TIME`, `DIM_TOWER`, etc.)
- KPI query scripts (Daily Call Volume, Revenue, International Call Monitoring, and other KPIs)
- Optional seed/load SQL scripts for validation

Recommended names:

- `01_create_dimensions.sql`
- `02_create_fact.sql`
- `03_kpi_queries.sql`
